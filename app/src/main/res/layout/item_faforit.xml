<?xml version="1.0" encoding="utf-8"?>
<!-- Card Item Tugas Favorit -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/card_color"
    app:cardCornerRadius="25dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Tipe Item (Tugas / Catatan) -->
        <TextView
            android:id="@+id/tvTipe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/item_type_task"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Icon Bintang (Status Favorit Visual) -->
        <ImageView
            android:id="@+id/ivBintangVisual"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:contentDescription="@string/cd_favorite_status"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_star_in"
            app:layout_constraintTop_toBottomOf="@id/tvTipe"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Judul -->
        <TextView
            android:id="@+id/tvJudul"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:text="@string/item_title_sample"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/ivBintangVisual"
            app:layout_constraintEnd_toStartOf="@id/ic_hapus_Faforit"
            app:layout_constraintTop_toTopOf="@id/ivBintangVisual"
            app:layout_constraintBottom_toTopOf="@id/tvDeskripsi"
            app:layout_constraintVertical_chainStyle="packed" />

        <!-- Deskripsi -->
        <TextView
            android:id="@+id/tvDeskripsi"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/item_description_sample"
            android:textColor="@color/text_secondary"
            android:textSize="15sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="@id/tvJudul"
            app:layout_constraintEnd_toEndOf="@id/tvJudul"
            app:layout_constraintTop_toBottomOf="@id/tvJudul"
            app:layout_constraintBottom_toBottomOf="@id/ivBintangVisual" />

        <!-- Tombol Hapus -->
        <ImageButton
            android:id="@+id/ic_hapus_Faforit"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_delete_task"
            android:scaleType="centerInside"
            android:src="@drawable/ic_delete"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintVertical_bias="0.5"
            app:tint="@color/icon_tint" />

        <!-- Tanggal -->
        <TextView
            android:id="@+id/tvTanggal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/item_date_sample"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
